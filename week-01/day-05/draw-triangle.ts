'use strict';

let lineCount: number = 4;
let triangle: string = '';
let triangleComponent: string = '*';
let a: number = 1;

for (let i: number = 1; i <=lineCount; i++) {
    triangle = triangle + triangleComponent;
    console.log(triangle);
}